//DISC version will stay 0.# until it completely meets the standard for the REST and gateway protocol.
const char *libver = "0.1";

//the gateway protocol DISC was compiled with
#define gatewayprotocol 6
